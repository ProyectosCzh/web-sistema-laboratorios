---
import Layout from "../../layouts/Layout.astro";
import ClassroomDetailPage from "../../islands/ClassroomDetailPage.tsx";

const { id } = Astro.params;
---

<Layout title="Detalle de aula | LABMANAGE">
  {id && <ClassroomDetailPage classroomId={id} client:only="react" />}
</Layout>
